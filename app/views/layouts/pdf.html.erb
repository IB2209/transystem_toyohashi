<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title><%= content_for?(:title) ? yield(:title) : "PDF出力" %></title>
    <style>
      body {
        font-family: "Hiragino Kaku Gothic ProN", sans-serif;
        font-size: 12px;
        color: #333;
        margin: 20px;
      }

      h1, h2 {
        color: #2c3e50;
        border-bottom: 1px solid #ccc;
        padding-bottom: 4px;
        margin-bottom: 15px;
      }

      table {
        width: 100%;
        border-collapse: collapse;
        margin-bottom: 20px;
      }

      th, td {
        border: 1px solid #999;
        padding: 6px;
        font-size: 12px;
        text-align: center;
      }

      th {
        background-color: #2c3e50;
        color: white;
      }

      tr:nth-child(even) {
        background-color: #f2f2f2;
      }

      .record-section {
        margin-bottom: 30px;
      }
    </style>
  </head>
  <body>
    <%= yield %>
  </body>
</html>
